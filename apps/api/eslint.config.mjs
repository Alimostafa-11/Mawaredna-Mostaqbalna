import js from '@eslint/js';
import tseslint from 'typescript-eslint';

export default tseslint.config(
  {
    ignores: ['dist/**', 'node_modules/**', 'eslint.config.mjs'],
  },
  js.configs.recommended,
  ...tseslint.configs.recommended,
  {
    languageOptions: {
      parserOptions: {
        projectService: true,
        tsconfigRootDir: import.meta.dirname,
      },
    },
    rules: {
      // Nest resolves providers from decorator metadata, which the rule reads
      // as an unused type-only import.
      '@typescript-eslint/no-unused-vars': [
        'error',
        { argsIgnorePattern: '^_', varsIgnorePattern: '^_' },
      ],
      // Mongoose lean() and aggregate() legitimately produce loosely-typed
      // results that are narrowed at the call site.
      '@typescript-eslint/no-explicit-any': 'warn',
    },
  },
);
