Static assets live here (logo, favicon, Open Graph image).

robots.txt and sitemap.xml are generated by the app at src/app/robots.ts and
src/app/sitemap.ts, so do not add static copies of those here.
